import { Meta } from '@/layouts/Meta';
import { Main } from '@/templates/Main';

const Blog = () => (
  <Main meta={<Meta title="Blog" description="Dan0xE Blog" />}>
    <p>No blog posts yet. Check back later!</p>
  </Main>
);

export default Blog;
